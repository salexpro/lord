var video = (function (exports) {
    'use strict';

    /* global YT */
    // const videoButton = document.getElementById('videoPlayPause');
    // videoButton.addEventListener('click', function () {
    //     this.classList.toggle('is_paused')
    //     player.playVideo();
    // })
    var player = false;
    var videoPlayer = document.querySelector('.video_player');
    var videoPlayerImg = document.querySelector('.video_player_img');

    var onPlayerReady = function onPlayerReady() {
      Array.from(document.querySelectorAll('.video_item--small')).forEach(function (video) {
        video.addEventListener('click', function () {
          var currId = video.getAttribute('data-id');
          var playerId = videoPlayer.getAttribute('data-id');
          videoPlayer.setAttribute('data-id', currId);
          videoPlayerImg.firstChild.setAttribute('src', "https://img.youtube.com/vi/".concat(currId, "/maxresdefault.jpg"));
          player.loadVideoById(currId);
          player.stopVideo();
          video.setAttribute('data-id', playerId);
          video.firstChild.setAttribute('src', "https://img.youtube.com/vi/".concat(playerId, "/maxresdefault.jpg"));
        });
      });
    };

    var ytready = function ytready() {
      player = new YT.Player('videoPlayer', {
        videoId: videoPlayer.getAttribute('data-id'),
        playerVars: {
          'rel': 0,
          'showinfo': 0,
          'modestbranding': 1
        },
        events: {
          'onReady': onPlayerReady
        }
      });
    };

    exports.ytready = ytready;

    return exports;

}({}));
//# sourceMappingURL=video.min.js.map
